# Exclude LLM Reasoning from Benchmark Retrieval

The benchmark path returns ranked Video Moments with scores and does not use LLM reasoning. Reasoning may remain as a developer-only analysis feature, but NTCIR-style evaluation should measure retrieval quality directly rather than allowing an LLM to explain, select, or adjust retrieved moments.
